(include "./widgets/separator/separator.yuck")
(include "./widgets/utils/utils.yuck")

(defwidget powermenu []
  (box :orientation "v"
       :class "powermenu"
    (centerbox :orientation "h" :class "container"
      (picker)
      (picker)
      (picker))
    (centerbox :orientation "h" :class "container"
      (picker)
      (picker)
      (picker))
    (centerbox :orientation "h" :class "container"
      (picker)
      (picker)
      (picker))))

(defwidget placeholder []
(picker))
