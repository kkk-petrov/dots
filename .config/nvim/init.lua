CONFIG = {
  showmode = false, --- e.g. -- INSERT --
  enable_mouse = true,
  transparency = true,
  cursorline = false,
  global_statusline = true,
  virtual_text = true,
  relativenumber = false,
  hide_cmdline = true
}

require("core")
require("lsp")
require("ui")
