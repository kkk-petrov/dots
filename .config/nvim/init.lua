CONFIG = {
  showmode = false, --- e.g. -- INSERT --
  enable_mouse = true,
  transparency = false,
  cursorline = false,
  global_statusline = true,
  virtual_text = true,
  relativenumber = false,
}

require("core")
require("lsp")
require("ui")
